<%

# BioCatalogue: app/views/curation/services_missing_annotations.html.erb
#
# Copyright (c) 2010, University of Manchester, The European Bioinformatics 
# Institute (EMBL-EBI) and the University of Southampton.
# See license.txt for details

%>

<%= render :partial => 'header', :locals => { :sub => "- Reports - Services Missing Annotations" } %>

<div class="box_info_standout center" style="margin: 1.5em auto; width: 680px;">
  This will identify all the services that don't have any values/annotations for a given field name.
</div>

<div style="margin: 0 auto; width: 700px;">

   <p class="center">
     <%= form_tag curation_reports_services_missing_annotations_path, :class => "box_form center" do %>
       Find all services that don't have 
       <%= select_tag("attribute_name", 
                      options_for_select({ "Descriptions" => "description",
                                           "Documentation URLs" => "documentation_url" }, params[:attribute_name])) -%>
       <%= submit_tag "GO" %>
     <% end %>
   </p>
   
   <% unless @services.nil?%>
   
     <p class="center" style="font-weight: bold; font-size: 108%;">
       <%= pluralize(@services.length, "service") -%> found
     </p>
     
     <ul>
       <% @services.each do |s| %>
         <li class="center" style="margin-bottom: 0.6em;"><%= link_to display_name(s), s, :popup => true -%></li>
       <% end %>
     </ul>
   <% end %>
   
 </div>
     
     
    
