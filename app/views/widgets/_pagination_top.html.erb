<%

   # BioCatalogue: app/views/widgets/_pagination_top.html.erb
   #
   # Copyright (c) 2008, University of Manchester, The European Bioinformatics
   # Institute (EMBL-EBI) and the University of Southampton.
   # See license.txt for details

%>



<div class="pagination" style="margin-bottom: 30px">
  <div class="page_info">
    <% if local_assigns.has_key?(:entry_name) %>
        <%= page_entries_info(paginated_items, :entry_name => entry_name) -%>
    <% else %>
        <%= page_entries_info(paginated_items) -%>
    <% end %>
  </div>
  <%= will_paginate(paginated_items,
                    :renderer => PaginationListLinkRenderer,
                    :previous_label => "« Previous",
                    :next_label => "Next »") -%>
</div>