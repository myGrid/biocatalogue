<%

# BioCatalogue: app/views/soap_services/bulk_new.html.erb
#
# Copyright (c) 2008, University of Manchester, The European Bioinformatics 
# Institute (EMBL-EBI) and the University of Southampton.
# See license.txt for details

%>

<h1>Submit services</h1>

<%= form_for @soap_service, :url =>{:controller => 'soap_services',:action => "bulk_create"}  do |f| %>
    <% if !@soap_service.nil? && !@soap_service.errors.nil? then %>
        <% @soap_service.errors.full_messages.each do |err| %>
            <%= err %>
        <% end %>
    <% end %>
  <p> List the wsdl URLs for the services you want to submit in the box below. <br />
      Each wsdl URL should on a separate line
	 
  </p>
  <p>
    <%= text_area_tag 'url_list', "list of description documents goes here.",:size => "85x10" -%>
  </p>
  
  <p>
    <%= submit_tag 'submit',  :url => { :action => 'bulk_create' } -%>

  </p>
<% end %>

<%= link_to 'Back', services_path %>



