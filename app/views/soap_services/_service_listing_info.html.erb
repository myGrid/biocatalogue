<%

# BioCatalogue: app/views/soap_services/_service_listing_info.html.erb
#
# Copyright (c) 2008, University of Manchester, The European Bioinformatics 
# Institute (EMBL-EBI) and the University of Southampton.
# See license.txt for details

%>

<div class="extra_detail_box">
	
	<% unless (soaplab_server = service_version_instance.service.soaplab_server).nil? %>
		<b>Soaplab Server Base URL:</b>
		<%= link_to(h(soaplab_server.endpoint), soaplab_server.endpoint, :popup => true) -%>
		<span class="separator">|</span>
	<% end %>
	
	<b>WSDL Location:</b>
	<%= link_to(h(service_version_instance.wsdl_location), service_version_instance.wsdl_location, :popup => true) -%>
	<span class="separator">|</span>
	
	<b><%= pluralize(service_version_instance.soap_operations.size, "SOAP Operation") -%></b>
	
</div>

