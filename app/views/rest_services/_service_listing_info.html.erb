<%

# BioCatalogue: app/views/rest_services/_service_listing_info.html.erb
#
# Copyright (c) 2008, University of Manchester, The European Bioinformatics 
# Institute (EMBL-EBI) and the University of Southampton.
# See license.txt for details

%>

<div class="extra_detail_box">
	
	<% unless (base_endpoint = service_version_instance.service.latest_deployment.endpoint).nil? %>
		<b>Base URL:</b>
		<%= link_to(h(base_endpoint), base_endpoint, :popup => true) -%>
		|
	<% end %>
	
  <% count = 0 %>
  <% service_version_instance.rest_resources.each { |r| count += r.rest_methods.size } %>
	<b><%= pluralize(count, "Endpoint") -%></b>
	
</div>


