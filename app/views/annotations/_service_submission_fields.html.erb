<%

# BioCatalogue: app/views/annotations/_service_submission_fields.html.erb
#
# Copyright (c) 2008, University of Manchester, The European Bioinformatics 
# Institute (EMBL-EBI) and the University of Southampton.
# See license.txt for details

%>

<%# Set defaults for optional parameters to this partial... %>
<% fields_disabled = false unless local_assigns.has_key?(:fields_disabled) %>

<!-- Name alias -->
<p class="high_linespaced">
	Add a name alias (ie: an alternative name for this service):
	<br/>
	<%= text_field_tag "annotations[name]", params[:annotations][:name], :disabled => fields_disabled, :style => "width: 98%;" %>
</p>

<!-- Documentation URL -->
<p class="high_linespaced">
	Add a URL to additional documentation for this service:
	<br/>
	<%= text_field_tag "annotations[documentation_url]", params[:annotations][:documentation_url], :disabled => fields_disabled, :style => "width: 98%;" %>
</p>

<!-- Description -->
<p class="high_linespaced">
	Add a description of this service:
	<br/>
	<%= text_area_tag "annotations[description]", params[:annotations][:description], :disabled => fields_disabled, :size => "50x10", :style => "width: 98%;" %>
</p>

<!-- Tags -->
<p class="high_linespaced">
	Add tags/keywords to describe this service (separated by commas):
	<br/>
	<%= text_field_tag "annotations[tags]", params[:annotations][:tags], :id => "tags_field", :disabled => fields_disabled, :style => "width: 98%;", :autocomplete => "off"  %>
	<div class="auto_complete" id="tags_auto_complete"></div>
	<%= image_tag "spinner.gif", :id => "addtag_indicator", :style => "display: none;" %>
	<%= auto_complete_field :tags_field,
			:update => "tags_auto_complete",
			:url => tags_auto_complete_url,
			:tokens => ',',
			:indicator => "addtag_indicator" -%>
</p>

