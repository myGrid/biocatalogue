<%

# BioCatalogue: app/views/registries/index.html.erb
#
# Copyright (c) 2009, University of Manchester, The European Bioinformatics 
# Institute (EMBL-EBI) and the University of Southampton.
# See license.txt for details

%>

<% t "Registries" -%>

<h1>
	Registries
	<%= info_icon_with_tooltip("These are the other registries that we connect to and harvest metadata from") -%>
</h1>

<%= render :partial => 'widgets/pagination_top', :locals => { :paginated_items => @registries } %>

<div id="latest_registries" style="margin: 1em 0;">
	<%= render :partial => "listing", :locals => { :items => @registries } %>
</div>

<%= render :partial => 'widgets/pagination_bottom', :locals => { :paginated_items => @registries } %>
